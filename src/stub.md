# Package stub

Empty entry point for the future product package. Intentionally no runtime logic.

When a language/stack is chosen, replace this file with a real module and document run/test commands in `AGENTS.md` (via `bmad-project-context`).
