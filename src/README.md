# Source tree

Clean-slate product root. No implementation yet.

Replace this stub when a stack and package layout are decided via BMAD architecture. Until then:

- Do not invent production behavior here.
- Keep methodology (`_bmad/`) out of this tree.
- Prefer small, verified entry points over speculative scaffolding.
